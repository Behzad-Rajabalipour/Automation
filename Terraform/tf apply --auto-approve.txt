tf apply --auto-approve 

tf apply --auto-approve -var prefix="behzad_week3" -var cidr_block_vpc="10.1.0.0/16" -var cidr_block_subnet="10.1.1.0/24"

tf apply --auto-approve -var-file="terraform.tfvars"

tf output

1. ssh -i Lab3 ec2-user@3.80.102.168
2. sudo vi /var/log/cloud-init-output.log 			# see user data installed inside the instance


tf init -migrate-state		=> if you removed backend(s3) then you have to use this code. 
tf init -reconfigure

If we comment/remove default in variables.tf, we give value in terminal like this => 
terraform apply -auto-approve -var='default_tags={Owner="behzad", App="Web"}' -var='prefix=prod' -var='public_subnet_cidrs=["10.10.0.0/24", "10.10.1.0/24"]' -var='vpc_cidr=10.10.0.0/16' -var='env=prod'